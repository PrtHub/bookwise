/** @type {import('tailwindcss').Config} */
export default {
  // This is an empty config file for Tailwind CSS v4
  // The actual configuration is now in your CSS files using the @theme directive
};
